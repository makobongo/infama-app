doctype html
html
  head
    title= title
    link(rel='stylesheet', href='/stylesheets/style.css')
    style(type='text/css').
       #body{
         text-align:center;
       }
       .btn {
            background-color: orange;
            color: black;
            font-size: 16px;
            padding: 16px 30px;
            border: none;
            cursor: pointer;
            border-radius: 5px;
            text-align: center;
          }
        .btn:hover {
            background-color: green;
            color: white;
          }
  body
    block content
